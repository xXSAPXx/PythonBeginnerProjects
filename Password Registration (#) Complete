# Вкарваме всичко в while loop. Насочваме клинта за параметрите на нужната парола.

while True:
    password=str(input("Please enter a password\nMinimal of 3 numbers\nNeeds to be 8 characters long.\nMust countain at least 2 upper cases!"))
    
# Бройм нужните големи букви и цифри използвани в паролата.

    upper_cases = sum(map(str.isupper, password))
    numbers = sum(map(str.isdigit, password))

# Използваме If Statements за да видим дали въведената парола отговаря на предварително-
# посочените изисквания. Ако паролата отговаря ТО тогава излизаме от loop-па.

    if len(password) < 8:
        print ("Not long enough!")
        continue 

    elif upper_cases < 2:
        print("Not enough upper cases!")
        continue
    
    elif numbers < 3:
        print("Not enough numbers!")
        continue
    
    password_two=str(input("Please repeat your password!"))
    if password != (password_two):
        print("The passwords don't match!")
        continue
    else:
        print("Congratulations! You succefully managed to register your account!")
        quit()