
password="12345"

# Дефинираме 3 опита за въвеждане на вярна парола! - for in range(3)

for userpass in range(3):
    user_input=input("What is the master password? ")
    
    if user_input != password:
        print ("Wrong password")
    elif user_input == password:
        break
    
# Ако 2те условия не са изпълнени едновремменно: (access denied)

if userpass == 2 and user_input != password:
    print("You have been denied access!!!")
    quit()
else:
    print ("Welcome the the spy network pleb")
